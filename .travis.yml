language: php
sudo: required
dist: trusty
group: edge

php:
  - hhvm
  - hhvm-nightly

matrix:
  allow_failures:
  - php: hhvm-nightly

before_script: composer install

script:
  - vendor/bin/hacktest tests
